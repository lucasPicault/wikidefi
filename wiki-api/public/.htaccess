<IfModule mod_rewrite.c>
    RewriteEngine On

    # Assure que les requêtes sont redirigées correctement
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>
